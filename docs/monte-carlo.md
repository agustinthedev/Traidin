# Monte Carlo

Monte Carlo resamples the realised net-P&L sequence using a persisted linear-congruential seed. It reports final-equity percentiles, drawdown percentiles and probability of ending above initial equity. Reusing the same seed and run snapshot reproduces the result.

