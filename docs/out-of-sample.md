# Out of Sample

The current Standard profile stores an explicit chronological split percentage, then runs a separate simulation that begins at the hold-out boundary while retaining only preceding closed-candle feature context. It reports independent OOS trade count, net P&L, expectancy, profit factor and drawdown. It never mixes future observations into the in-sample test.
